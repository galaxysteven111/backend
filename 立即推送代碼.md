# 🚀 立即推送代碼到 GitHub

## 📋 你的倉庫：https://github.com/galaxysteven111/backend

目前倉庫是空的，需要推送代碼。

---

## ⚡ 快速步驟

### 在 Cursor 終端中執行以下命令：

#### 步驟 1：檢查 Git 狀態

```bash
git status
```

#### 步驟 2：如果還沒有初始化 Git

```bash
git init
git add .
git commit -m "初始提交：捐飯盒平台"
```

#### 步驟 3：連接到 GitHub 倉庫

```bash
git remote add origin https://github.com/galaxysteven111/backend.git
git branch -M main
```

#### 步驟 4：推送到 GitHub

```bash
git push -u origin main
```

---

## 🔐 如果遇到認證問題

### 方法 1：使用 Personal Access Token（推薦）

1. **訪問**：https://github.com/settings/tokens
2. **點擊** "Generate new token (classic)"
3. **設置**：
   - Note: `Railway Deploy`
   - Expiration: `90 days`（或你選擇的時間）
   - 勾選 `repo` 權限
4. **點擊** "Generate token"
5. **複製 token**（只顯示一次，要保存好）
6. **推送時**：
   - Username: 你的 GitHub 用戶名
   - Password: 使用剛才複製的 token（不是 GitHub 密碼）

### 方法 2：使用 GitHub Desktop

1. 下載 GitHub Desktop：https://desktop.github.com
2. 登錄你的 GitHub 帳號
3. 添加本地倉庫
4. 點擊 "Publish repository"

---

## ✅ 推送成功的標誌

推送成功後，訪問 https://github.com/galaxysteven111/backend，你會看到：
- ✅ 文件列表（backend/, frontend/, package.json 等）
- ✅ README.md（如果有）
- ✅ 提交歷史

---

## 🎯 推送完成後

推送成功後，立即在 Railway 部署：

1. **訪問** https://railway.app
2. **使用 GitHub 登錄**
3. **點擊** "New Project" → "Deploy from GitHub repo"
4. **選擇** `galaxysteven111/backend`
5. **設置** Root Directory（見下方說明）

---

## ⚠️ Root Directory 設置

**重要**：需要確認你的倉庫結構

### 情況 A：如果整個項目都在倉庫根目錄

```
backend/          ← 倉庫根目錄
  ├── src/
  ├── package.json
  ├── frontend/
  └── ...
```

**Root Directory** = `.`（當前目錄，即倉庫根目錄）

### 情況 B：如果後端代碼在 backend/ 子目錄

```
foodbox-platform/  ← 倉庫根目錄
  ├── backend/     ← 後端代碼在這裡
  │   ├── src/
  │   └── package.json
  └── frontend/
```

**Root Directory** = `backend`

---

## 📝 完整命令（一次性複製）

```bash
# 1. 初始化 Git（如果還沒有）
git init
git add .
git commit -m "初始提交：捐飯盒平台"

# 2. 連接 GitHub
git remote add origin https://github.com/galaxysteven111/backend.git
git branch -M main

# 3. 推送
git push -u origin main
```

---

## 🆘 常見問題

### 問題 1：`remote origin already exists`

**解決**：
```bash
git remote remove origin
git remote add origin https://github.com/galaxysteven111/backend.git
```

### 問題 2：認證失敗

**解決**：使用 Personal Access Token（見上方方法 1）

### 問題 3：推送被拒絕

**解決**：
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

---

**現在開始推送！執行上述命令後告訴我結果。** 🚀
