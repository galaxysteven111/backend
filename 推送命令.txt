📋 在 Cursor 終端中執行以下命令

═══════════════════════════════════════════════════════

步驟 1：初始化 Git（如果還沒有）
───────────────────────────────────────────────────────
git init
git add .
git commit -m "初始提交：捐飯盒平台"

═══════════════════════════════════════════════════════

步驟 2：連接到 GitHub 倉庫
───────────────────────────────────────────────────────
git remote add origin https://github.com/galaxysteven111/backend.git
git branch -M main
git push -u origin main

═══════════════════════════════════════════════════════

如果遇到認證問題：
───────────────────────────────────────────────────────
1. 訪問：https://github.com/settings/tokens
2. 點擊 "Generate new token (classic)"
3. 選擇 "repo" 權限
4. 生成並複製 token
5. 推送時使用 token 作為密碼

═══════════════════════════════════════════════════════

推送成功後：
───────────────────────────────────────────────────────
1. 訪問 https://railway.app
2. 使用 GitHub 登錄
3. 選擇 "Deploy from GitHub repo"
4. 選擇 galaxysteven111/backend
5. 設置 Root Directory（見下方說明）

═══════════════════════════════════════════════════════

Root Directory 設置說明：
───────────────────────────────────────────────────────

如果倉庫結構是：
backend/          ← 倉庫根目錄
  ├── src/
  ├── package.json
  └── ...

那麼 Root Directory = . （當前目錄，即倉庫根目錄）

如果倉庫結構是：
foodbox-platform/  ← 倉庫根目錄
  ├── backend/     ← 後端代碼
  └── frontend/

那麼 Root Directory = backend

═══════════════════════════════════════════════════════
