# 📤 推送代碼到 GitHub

## ❌ 目前狀態：倉庫是空的

你的 GitHub 倉庫 `galaxysteven111/backend` 目前是空的，需要推送代碼。

---

## ⚡ 最簡單的方式：運行批處理文件

### 在 Cursor 終端中執行：

```cmd
.\推送代碼到GitHub.bat
```

這會自動完成所有步驟！

---

## 📋 或者：手動執行命令

### 步驟 1：初始化 Git（如果還沒有）

```bash
git init
git add .
git commit -m "初始提交：捐飯盒平台"
```

### 步驟 2：連接到 GitHub 倉庫

```bash
git remote add origin https://github.com/galaxysteven111/backend.git
git branch -M main
```

**如果已經有 remote**：
```bash
git remote set-url origin https://github.com/galaxysteven111/backend.git
```

### 步驟 3：推送到 GitHub

```bash
git push -u origin main
```

---

## 🔐 認證問題解決

### 如果推送時要求輸入用戶名和密碼：

1. **訪問**：https://github.com/settings/tokens
2. **點擊** "Generate new token (classic)"
3. **設置**：
   - Note: `Deploy Token`
   - Expiration: `90 days`
   - **勾選** `repo` 權限
4. **點擊** "Generate token"
5. **複製 token**（只顯示一次！）
6. **推送時**：
   - Username: `galaxysteven111`
   - Password: **使用 token**（不是 GitHub 密碼）

---

## ✅ 推送成功的標誌

推送成功後，訪問 https://github.com/galaxysteven111/backend，你會看到：

- ✅ 文件列表（backend/, frontend/, package.json 等）
- ✅ 提交歷史
- ✅ 不再是空倉庫

---

## 🎯 推送完成後

推送成功後，立即在 Railway 部署：

1. **訪問** https://railway.app
2. **使用 GitHub 登錄**
3. **點擊** "New Project" → "Deploy from GitHub repo"
4. **選擇** `galaxysteven111/backend`
5. **設置** Root Directory（見下方）

---

## ⚠️ Root Directory 設置

因為你的倉庫名稱是 `backend`，但項目結構是：

```
backend/          ← 這可能是倉庫根目錄
  ├── backend/    ← 後端代碼
  ├── frontend/   ← 前端代碼
  └── ...
```

**需要確認**：
- 如果整個項目（包括 backend/ 和 frontend/）都在倉庫根目錄
  - Root Directory = `.`（當前目錄）
- 如果只有後端代碼在倉庫根目錄
  - Root Directory = `.`（當前目錄）

---

## 🚀 現在開始

**推薦**：運行批處理文件
```cmd
.\推送代碼到GitHub.bat
```

**或者**：手動執行上述命令

**執行後告訴我結果！** 🎉
